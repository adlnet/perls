User Email Access
=====================

This module provides a new Views Field handler that bypasses the built in access
contraints on the user email field. This module expands the access to allow more
flexibility in which users can view their own or other user's email addresses.
